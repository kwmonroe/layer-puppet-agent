repo: https://github.com/jamesbeedy/layer-puppet-agent.git
includes:
  - 'layer:basic'
  - 'layer:apt'
defines:
  puppet-version:
    type: string
    default: '4'
    description: |
      Puppet version choices are `3` or `4`.
